package com.example.myapplication.View;
/*
import static org.junit.Assert.*;
import android.view.View;
import org.junit.Before;
import org.junit.Test;
import org.robolectric.Robolectric;

public class PlayerHealthTest {
    private GameScreenActivity gameScreenActivity;

    @Before
    public void setUp() {
        gameScreenActivity = Robolectric.buildActivity(GameScreenActivity.class).create().resume().get();
    }

    @Test
    public void testHealthGainOnHealthBoostCollision() {
        // Assuming you have a method to set up the health boost ImageView
        gameScreenActivity.setUpHealthBoost();

        // Call onPlayerMove with coordinates that collide with the health boost
        gameScreenActivity.onPlayerMove(500, 500);

        // Verify that the player's health has increased by 50
        assertEquals(150, gameScreenActivity.getPlayerHP());

        // Verify that the health boost visibility is set to INVISIBLE
        assertEquals(View.INVISIBLE, gameScreenActivity.getHealthBoost().getVisibility());
    }
    @Test
    public void testHealthGainOnHealthBoostCollision() {
        // Assuming you have a method to set up the health boost ImageView
        gameScreenActivity.setUpHealthBoost();

        // Call onPlayerMove with coordinates that collide with the health boost
        gameScreenActivity.onPlayerMove(600, 600);

        // Verify that the player's health has increased by 50
        assertEquals(150, gameScreenActivity.getPlayerHP());

        // Verify that the health boost visibility is set to INVISIBLE
        assertEquals(View.INVISIBLE, gameScreenActivity.getHealthBoost().getVisibility());
    }

}

 */
