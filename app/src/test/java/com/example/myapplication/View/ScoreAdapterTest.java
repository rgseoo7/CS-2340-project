package com.example.myapplication.View;
/*
import static org.junit.Assert.*;
import com.example.myapplication.Model.ScoreEntry;
import org.junit.Test;
import java.util.List;
import java.util.ArrayList;
public class ScoreAdapterTest {

    @Test
    public void testScoreAdapter() {
        // Create a list of ScoreEntries
        List<ScoreEntry> scoreEntries = new ArrayList<>();
        scoreEntries.add(new ScoreEntry("Player1", 105.0));
        scoreEntries.add(new ScoreEntry("Player2", 160.0));
        scoreEntries.add(new ScoreEntry("Player3", 210.0));

        // Create a ScoreAdapter with the initial list
        ScoreAdapter scoreAdapter = new ScoreAdapter(scoreEntries);

        // Verify that the adapter has the correct initial item count
        assertEquals(scoreEntries.size(), scoreAdapter.getItemCount());

        // Update the score of the first player (assuming there's a method in ScoreEntry to update the score)
        scoreEntries.get(0).updateScore(120);

        // Notify the adapter that the data set has changed
        scoreAdapter.notifyDataSetChanged();

        // Verify that the score in the adapter has been updated
        assertEquals("120", scoreAdapter.scoreEntries.get(0).getScore()); // Assuming getScore returns a String
    }
 @Test
    public void testScoreAdapter() {
        // Create a list of ScoreEntries
        List<ScoreEntry> scoreEntries = new ArrayList<>();
        scoreEntries.add(new ScoreEntry("Player1", 1690.0));
        scoreEntries.add(new ScoreEntry("Player2", 120.0));
        scoreEntries.add(new ScoreEntry("Player3", 250.0));

        // Create a ScoreAdapter with the initial list
        ScoreAdapter scoreAdapter = new ScoreAdapter(scoreEntries);

        // Verify that the adapter has the correct initial item count
        assertEquals(scoreEntries.size(), scoreAdapter.getItemCount());

        // Update the score of the first player (assuming there's a method in ScoreEntry to update the score)
        scoreEntries.get(0).updateScore(100);

        // Notify the adapter that the data set has changed
        scoreAdapter.notifyDataSetChanged();

        // Verify that the score in the adapter has been updated
        assertEquals("120", scoreAdapter.scoreEntries.get(0).getScore()); // Assuming getScore returns a String
    }
}

 */